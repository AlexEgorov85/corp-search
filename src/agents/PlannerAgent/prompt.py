# src/agents/PlannerAgent/prompt.py
"""
Модуль промптов для PlannerAgent — агента, отвечающего за декомпозицию
исходного пользовательского вопроса на последовательность атомарных подвопросов.

ОСНОВНЫЕ ЦЕЛИ ПЛАНИРОВЩИКА:
1. Разбить сложный вопрос на логически связанные, независимые подвопросы.
2. Установить зависимости между подвопросами (например, q2 зависит от q1).
3. Сформировать структурированный план, пригодный для последующего выполнения
   ReasonerAgent и Executor.

СТРУКТУРА ИТОГОВОГО JSON-ОТВЕТА:
{
  "reasoning": [
    "P1: Какова конечная цель пользователя?",
    "P2: Можно ли ответить за один шаг или требуется многошаговый план?",
    "P3: Какие промежуточные данные нужны для достижения цели?",
    "P4: В каком порядке должны выполняться шаги?",
    "P5: Какой тип операции подходит для каждого шага: DIRECT или SEMANTIC?"
  ],
  "planning": {
    "needed": true|false,
    "confidence": 0.0–1.0,
    "reason": "Краткий технический ответ на P1–P5",
    "explanation": "Человекочитаемое обоснование необходимости декомпозиции"
  },
  "subquestions": [
    {
      "id": "q1",
      "text": "Атомарный подвопрос",
      "depends_on": [],
      "confidence": 0.0–1.0,
      "reason": "Краткий ответ на P1–P5",
      "explanation": "Человекочитаемое обоснование (1–2 предложения)"
    }
  ],
  "final_decision": {
    "explanation": "Итоговое резюме в 1–2 предложения"
  }
}

ВАЖНО:
- Все поля с `confidence` сопровождаются двумя пояснениями:
  - `reason` — техническое обоснование (для машинной логики)
  - `explanation` — человекочитаемое резюме (для логов и UI)
- Валидация сущностей НЕ требуется на этапе планирования — это задача Reasoner/Executor.
- Промпт строго запрещает любой текст вне JSON.
"""

import json
import textwrap


def get_decomposition_system_prompt() -> str:
    """
    Формирует системный промпт для LLM, задающий правила декомпозиции.

    Этот промпт определяет:
    - Что такое подвопрос (атомарный, фактологический)
    - Какие аналитические вопросы (P1–P5) должен ответить LLM
    - Какую структуру должен иметь итоговый JSON
    - Какие поля обязательны и как они должны быть заполнены

    Returns:
        str: Системный промпт в виде многострочной строки
    """
    # === ФОРМИРОВАНИЕ СИСТЕМНОГО ПРОМПТА ===
    # Используем textwrap.dedent для корректного форматирования отступов
    return textwrap.dedent("""\
        ТЫ — PlannerAgent в ReAct-системе. ТВОЯ ЗАДАЧА — ВЕРНУТЬ ТОЛЬКО ВАЛИДНЫЙ JSON.

        ### 🔍 Что такое подвопрос?
        Подвопрос — это **атомарный, фактологический вопрос**, на который можно дать однозначный ответ
        (список, скаляр, объект). Он не должен содержать глаголов в повелительном наклонении
        ("найди", "получи", "проанализируй").

        ### 📌 Обязательный анализ (ответь на P1–P5 и включи в "reasoning")
        P1. Какова конечная цель пользователя? (Что именно нужно вернуть в final_answer?)
        P2. Можно ли ответить за один шаг или требуется многошаговый план?
        P3. Какие промежуточные данные нужны для достижения цели?
        P4. В каком порядке должны выполняться шаги? (зависимости между подвопросами)
        P5. Какой тип операции подходит для каждого шага: DIRECT или SEMANTIC?

        ### 📏 СТРУКТУРА ОТВЕТА
        {
          "reasoning": ["P1: ...", "P2: ...", ..., "P5: ..."],
          "planning": {
            "needed": true|false,
            "confidence": 0.0–1.0,
            "reason": "...",
            "explanation": "Почему требуется/не требуется декомпозиция?"
          },
          "subquestions": [
            {
              "id": "q1",
              "text": "...",
              "depends_on": [],
              "confidence": 0.0–1.0,
              "reason": "Краткий ответ на P1–P5",
              "explanation": "Человекочитаемое обоснование (1–2 предложения)"
            }
          ],
          "final_decision": {
            "explanation": "Итоговое резюме в 1–2 предложения"
          }
        }

        ### ⚠️ ВАЖНО
        - НИКАКОГО ТЕКСТА ВНЕ JSON.
        - Начни с '{', закончи '}'.
        - Не используй markdown, пояснения, комментарии.
    """)


def get_decomposition_user_prompt(question: str, tool_registry: dict, feedback: str) -> str:
    """
    Формирует пользовательский промпт с контекстом для LLM.

    Содержит:
    - Исходный вопрос пользователя
    - Доступные инструменты (агенты и их операции)
    - Обратную связь от предыдущих попыток (если есть)

    Args:
        question (str): Исходный вопрос пользователя
        tool_registry (dict): Снимок реестра инструментов
        feedback (str): Обратная связь от предыдущих попыток генерации

    Returns:
        str: Пользовательский промпт в виде многострочной строки
    """
    # === ФОРМИРОВАНИЕ ИНФОРМАЦИИ ОБ ИНСТРУМЕНТАХ ===
    # Если реестр инструментов пуст — указываем это явно
    tools_info = (
        json.dumps(tool_registry, ensure_ascii=False, indent=2)
        if tool_registry
        else "Нет доступных инструментов"
    )

    # === СБОРКА ПОЛЬЗОВАТЕЛЬСКОГО ПРОМПТА ===
    # Используем f-строки для вставки динамических частей
    return (
        f"### ❓ Исходный вопрос\n{question}\n\n"
        f"### 📚 Доступные инструменты\n{tools_info}\n\n"
        f"### 🔄 Предыдущая обратная связь\n{feedback if feedback else 'Нет предыдущих попыток'}\n\n"
        "### 📜 Инструкция\n"
        "1. Выполни анализ через P1–P5\n"
        "2. Сформируй подвопросы, если planning.needed = true\n"
        "3. Укажи зависимости через depends_on\n"
        "4. Верни ТОЛЬКО JSON в требуемом формате"
    )